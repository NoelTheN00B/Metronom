package de.leon.metronom.CustomClasses.Timer;

import mobi.upod.timedurationpicker.TimeDurationUtil;

public class IncreaseBpmAfterDialogFragment extends TimerInputDialogFragment {

    /*
    @Override
    protected long getInitialDuration() {
        return 5 * 1000;
    }

    @Override
    protected void setInMain(long duration) {
        super.mainActivity.setIncreaseBpmAfterInMs(duration);
        super.mainActivity.getIncreaseBpmAfter().setText(TimeDurationUtil.formatMinutesSeconds(duration));
    }

     */
}
